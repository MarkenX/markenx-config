apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Trivy Operator for container image vulnerability scanning
# Scans images in the markenx namespace and generates VulnerabilityReports

resources:
  - namespace.yaml
  - trivy-crds.yaml
  - trivy-operator.yaml
